// 📝 Завдання для практики по темі Масиви
// 🔹 Рівень 1 (Базовий)

// Створи масив із 5 улюблених фруктів.

// const fruits = ["banana", "avocado", "apple", "cherry", "plum"];

// Виведи перший і останній елемент.

// console.log(fruits[0]);

// const lastElementIndex = fruits.length - 1;
// console.log(fruits[lastElementIndex]);

// Дізнайся довжину масиву.

// console.log(fruits.length);

// Є масив

// const numbers = [10, 20, 30, 40, 50];

// Виведи число з індексом 2.

// console.log(numbers[1]);

// Заміні його на 35.

// numbers[1] = 35;
// console.log(numbers);

// Створи порожній масив.

// const array = [];

// Додай у нього числа від 1 до 5 за допомогою push().

// array.push(1, 2, 3, 4, 5);
// console.log(array);

// Потім видали останній елемент (pop()).

// array.pop();
// console.log(array);

// =========================================================

// 🔹 Рівень 2 (Середній)

// Є масив

// const names = ["Anna", "Oleg", "Ira"];

// Додай ім’я "Petro" у кінець.

// names.push("Petro");
// console.log(names);

// Додай ім’я "Olga" на початок.

// names.unshift("Olga");
// console.log(names);

// Видали "Oleg" зі списку (пошукай метод).

// names.splice(2, 1);
// console.log(names);

// Є масив

// const nums = [1, 2, 3, 4, 5, 6];

// Зроби новий масив тільки з парними числами.

// let evenNums = [];

// for (let num of nums) {
//   if (num % 2 === 0) {
//     evenNums.push(num);
//   }
// }
// console.log(evenNums);

// Зроби новий масив тільки з непарними числами.

// let oddNums = [];

// for (let num of nums) {
//   if (num % 2 !== 0) {
//     oddNums.push(num);
//   }
// }
// console.log(oddNums);

// Є масив

// const words = ["JS", "is", "cool"];

// Об’єднай слова в один рядок "JS is cool" (підказка: метод join()).

// console.log(words.join(" "));

// =========================================================

// 🔹 Рівень 3 (Ускладнений)

// Напиши функцію removeEverySecond(arr),
// яка приймає масив і повертає новий масив без кожного другого елемента.

// function removeEverySecond(arr) {
//   const result = [];
//   for (let i = 0; i < arr.length; i += 2) {
//     result.push(arr[i]);
//   }
//   return result;
// }

// console.log(removeEverySecond([1, 2, 3, 4, 5])); // [1,3,5]

// Є масив чисел

// const data = [3, 7, 2, 9, 4];

// Знайди найбільше число.

// console.log(Math.max(...data)); // через оператор розпакування ... (spread)

// Знайди найменше число.

// let min = data[0]; // припускаємо, що найменший елемент — перший
// for (let num of data) {
//   // перебираємо всі елементи
//   if (num < min) {
//     // якщо поточне число менше, ніж min
//     min = num; // оновлюємо min
//   }
// }

// console.log(min);

// Напиши функцію sumArray(arr), яка повертає суму всіх чисел у масиві.

// function sumArray(arr) {
//   let sum = 0;
//   for (let i = 0; i < arr.length; i++) {
//     sum = sum + arr[i];
//   }
//   return sum;
// }

// console.log(sumArray([1, 2, 3])); // 6

// Напиши функцію reverseArray(arr), яка повертає масив у зворотному порядку.

// function reverseArray(arr) {
//   const result = [];
//   for (let i = arr.length - 1; i >= 0; i--) {
//     result.push(arr[i]);
//   }
//   return result;
// }

// console.log(reverseArray([1, 2, 3])); // [3,2,1]

// =========================================================

// 🔹 Рівень 4 (Для розвитку логіки)

// Є масив const arr = [5, 1, 5, 3, 5, 2];.

// Порахуй, скільки разів зустрічається число 5.

// Є масив const numbers = [1, 2, 3, 4, 5];.

// Створи новий масив, де кожне число піднесене до квадрату.

// [1,4,9,16,25]

// Напиши функцію unique(arr), яка прибирає дублікати з масиву.

// unique([1,2,2,3,3,4]); // [1,2,3,4]

// =========================================================

// ✅ Порада від викладача

// Починай із рівня 1 → коли стане легко, рухайся далі.

// Кожну задачу вирішуй самостійно, навіть якщо спочатку здається складною.

// Використовуй console.log() після кожної дії — це допоможе розуміти,
// що відбувається з масивом.

// ========================================================

// 🔹 Практичні завдання
// Блок 1. Основи масивів

// Створи масив чисел від 1 до 5.

// const numbers = [1, 2, 3, 4, 5];

// console.log(numbers);

// Виведи перший та останній елемент масиву.

// console.log(numbers[0]);

// console.log(numbers[numbers.length - 1]);

// Заміни другий елемент на 20.

// numbers[1] = 20;
// console.log(numbers);

// Додай число 6 в кінець масиву.

// numbers.push(6);
// console.log(numbers);

// Виведи масив та його довжину.

// console.log(numbers.length);

// =========================================

// Блок 2. Методи масивів

// В масиві ["apple", "banana", "orange"]:

// const fruits = ["apple", "banana", "orange"];

// Додай "kiwi" в кінець.

// fruits.push("kiwi");
// console.log(fruits);

// Видали перший елемент.

// fruits.shift();
// console.log(fruits);

// Об’єднай всі елементи в рядок через пробіл.

// console.log(fruits.join(" "));

// Для масиву чисел [3, 7, 2, 9, 4]:

// const numbers = [3, 7, 2, 9, 4];

// Знайди найбільше число.

// console.log(Math.max(...numbers));

// Знайди суму всіх чисел.

// let sum = 0;
// for (let i = 0; i < numbers.length; i++) {
//   sum = sum + numbers[i];
// }
// console.log(sum);

// Створи новий масив тільки з парними числами.

// let evenNums = [];
// for (let num of numbers) {
//   if (num % 2 === 0) {
//     evenNums.push(num);
//   }
// }
// console.log(evenNums);

// Для масиву [1, 2, 3, 4, 5]:

// const arr = [1, 2, 3, 4, 5];

// Переверни його порядок.
// console.log(`String: ${arr.join(", ")}`);

// Візьми тільки перші три елементи.
// console.log(arr.slice(0, 3));

// Блок 3. Ітерація по масиву

// Використовуючи for, виведи всі елементи масиву [10, 20, 30, 40].

// const arr1 = [10, 20, 30, 40];

// for (let i = 0; i < arr1.length; i++) {
//   console.log(arr1[i]);
// }

// Використовуючи for...of, знайди суму всіх елементів.
// let sum = 0;
// for (let elem of arr1) {
//   sum = elem + sum;
// }
// console.log(sum);

// Використовуючи while, виведи кожен другий елемент масиву [5, 10, 15, 20, 25, 30].

// const arr2 = [5, 10, 15, 20, 25, 30];

// let i = 1;

// while (i < arr2.length) {
//   console.log(arr2[i]);
//   i += 2;
// }

// ========================================

// Задача 1. Генератор slug

// Перш, ніж розв’язувати задачу, давай визначимося із новим терміном!

// Термін slug — це зрозумілий людині унікальний ідентифікатор,
// який використовується у веб розробці для створення читабельних URL - адрес.

//   Наприклад, замість того, щоб користувач побачив в адресному рядку mysite.com / posts / 1q8fh74tx,
//   можна зробити slug із назви статті.
//   У результаті адреса буде приємнішою для сприйняття: mysite.com / posts / arrays -for-beginners.

// Slug — це завжди рядок у нижньому регістрі, слова якого розділені тире.

// З цим розібралися? А тепер давай нарешті виконувати задачу!

// Напиши функцію slugify(title), яка приймає заголовок статті, параметр title і повертає slug,
//   створений із цього рядка.

// Значенням параметра title будуть рядки, слова яких розділені лише пробілами.
// Усі символи slug повинні бути в нижньому регістрі.
// Усі слова slug повинні бути розділені тире.
// Візьми код нижче і встав після оголошення своєї функції для перевірки коректності її роботи.
// У консоль будуть виведені результати її роботи.

// function slugify(title) {
//   let slug = title.toLowerCase().split(" ").join("-");
//   return slug;
// }

// console.log(slugify("Arrays for beginners")); // "arrays-for-beginners"
// console.log(slugify("English for developer")); // "english-for-developer"
// console.log(slugify("Ten secrets of JavaScript")); // "ten-secrets-of-javascript"
// console.log(slugify("How to become a JUNIOR developer in TWO WEEKS")); // "how-to-become-a-junior-developer-in-two-weeks"

// =============================================

// Задача 2. Композиція масивів

// Напиши функцію під назвою makeArray, яка приймає три параметри:
// firstArray(масив), secondArray(масив) і maxLength(число).
// Функція повинна створювати новий масив, який містить усі елементи з firstArray,
//   а потім усі елементи з secondArray.

// Якщо кількість елементів у новому масиві перевищує maxLength,
//   функція повинна повернути копію масиву з довжиною maxLength елементів.
// В іншому випадку функція повинна повернути весь новий масив.

// Візьми код нижче і встав після оголошення своєї функції для перевірки коректності її роботи.
// У консоль будуть виведені результати її роботи.

// function makeArray(firstArray, secondArray, maxLength) {
//   const newArray = firstArray.concat(secondArray).slice(0, maxLength);
//   return newArray;
// }

// console.log(makeArray(["Mango", "Poly"], ["Ajax", "Chelsea"], 3)); // ["Mango", "Poly", "Ajax"]
// console.log(makeArray(["Mango", "Poly", "Houston"], ["Ajax", "Chelsea"], 4)); // ["Mango", "Poly", "Houston", "Ajax"]
// console.log(makeArray(["Mango"], ["Ajax", "Chelsea", "Poly", "Houston"], 3)); // ["Mango", "Ajax", "Chelsea"]
// console.log(makeArray(["Earth", "Jupiter"], ["Neptune", "Uranus"], 2)); // ["Earth", "Jupiter"]
// console.log(makeArray(["Earth", "Jupiter"], ["Neptune", "Uranus"], 4)); // ["Earth", "Jupiter", "Neptune", "Uranus"]
// console.log(makeArray(["Earth", "Jupiter"], ["Neptune", "Uranus", "Venus"], 0)); // []

// =============================================

// Задача 3. Фільтрація масиву чисел

// Напиши функцію filterArray(numbers, value), яка приймає масив чисел(numbers) та значення(value) як параметри.
// Функція повинна повертати новий масив лише тих чисел із масиву numbers, які більші за значення value.

// Усередині функції:

// Створи порожній масив, у який будеш додавати підходящі числа.
// Використай цикл для ітерації кожного елемента масиву numbers.
// Використовуй умовний оператор if усередині циклу для перевірки кожного елемента и додавання до свого масиву.
// Поверни свій новий масив з підходящими числами як результат.

// Візьми код нижче і встав після оголошення своєї функції для перевірки коректності її роботи.
// У консоль будуть виведені результати її роботи.

// function filterArray(numbers, value) {
//   const arr = [];
//   for (let num of numbers) {
//     if (num > value) {
//       arr.push(num);
//     }
//   }
//   return arr;
// }

// console.log(filterArray([1, 2, 3, 4, 5], 3)); // [4, 5]
// console.log(filterArray([1, 2, 3, 4, 5], 4)); // [5]
// console.log(filterArray([1, 2, 3, 4, 5], 5)); // []
// console.log(filterArray([12, 24, 8, 41, 76], 38)); // [41, 76]
// console.log(filterArray([12, 24, 8, 41, 76], 20)); // [24, 41, 76]
