// 🔹 Завдання: Передаємо ім’я користувача через колбек

// Умова:
// Створи функцію greetUser(name, callback), яка передає ім’я користувача у колбек.
// Колбек повинен вивести повідомлення:

// Привіт, [ім'я]!

// Вимоги:

// Створи окрему функцію-колбек, наприклад sayHi.

// Передай її у greetUser.

// Не використовуй інлайн-функції.

// Твій колбек тут

// Функція, яка приймає ім'я та колбек
// function greetUser(name, callback) {
//   // Виклик колбека з переданим ім'ям
//   callback(name);
// }

// function sayHi(name) {
//   console.log(`Привіт, ${name}`);
// }

// Виклик функції
// greetUser("Аня", /* твій колбек тут */ sayHi);
// greetUser("Аня", sayHi);

// Підказка:

// Колбек повинен приймати параметр name і виводити повідомлення.

// Приклад виклику всередині колбека: console.log(Привіт, ${name}!);

// ===============================================================

// 🔹 Завдання: Множимо числа через колбек

// Умова:
// Створи функцію multiply(a, b, callback), яка приймає два числа a і b,
//     обчислює їх добуток і передає результат у колбек.

// Вимоги:

// Створи окрему функцію-колбек, наприклад showResult.

// Колбек повинен вивести повідомлення:

// Результат множення: [результат]

// Не використовуй інлайн-функції.

// Твій колбек тут
// function showResult(result) {
//   console.log(`Результат множення: ${result}`);
// }

// Функція, яка приймає два числа і колбек
// function multiply(a, b, callback) {
//   const result = a * b;
//   callback(result);
// }

// Виклик функції
// multiply(3, 5 /* твій колбек тут */ );
// multiply(3, 5, showResult);

// Підказка:

// Колбек має приймати один параметр — результат множення.

// Усередині колбека використай console.log для виводу.

// ===============================================================

// 🔹 Завдання: Перевірка числа через колбек

// Умова:
// Створи функцію checkNumber(number, callback), яка перевіряє, чи число більше 10. Колбек повинен вивести:

// "Більше 10", якщо число > 10

// "10 або менше", якщо число ≤ 10

// Вимоги:

// Створи окрему функцію-колбек, наприклад showCheckResult.

// Не використовуй інлайн-функції.

// Твій колбек тут

// Функція, яка перевіряє число
// function checkNumber(number, callback) {
//   const isGreater = number > 10;
//   callback(isGreater);
// }

// function showCheckResult(isGreater) {
//   if (isGreater === true) {
//     console.log("Більше 10");
//   } else {
//     console.log("10 або менше");
//   }
// }

// Виклики функції
// checkNumber(15, /* твій колбек тут */);
// checkNumber(15, showCheckResult);
// checkNumber(7, /* твій колбек тут */);
// checkNumber(7, showCheckResult);

// Підказка:

// Колбек має приймати булеве значення (true/false) і виводити відповідне повідомлення через console.log.

// ==========================================================

// 🔹 Міні-тест: Колбек-функції
// Завдання 1. Привітання

// Створи звичайну функцію-колбек sayHello, яка виводить:

// Привіт, друже!

// function sayHello() {
//   console.log("Привіт, друже!");
// }

// Потім створи функцію greet(callback) і передай їй колбек.

// function greet(callback) {
//   callback();
// }

// greet(sayHello);

// Завдання 2. Множення чисел

// Створи звичайну функцію-колбек showProduct, яка приймає два числа, множить їх і виводить:

// Добуток: [результат]

function showProduct(multiply) {
  console.log(`Добуток: ${multiply}`);
}

// Потім створи функцію multiplyNumbers(a, b, callback) і передай їй колбек.

function multiplyNumbers(a, b, callback) {
  const multiply = a * b;
  callback(multiply);
}

multiplyNumbers(2, 4, showProduct);

// Завдання 3. Перевірка числа

// Створи звичайну функцію-колбек checkEvenOdd, яка приймає число і виводить:

// "Парне", якщо число парне

// "Непарне", якщо число непарне

function checkEvenOdd(number) {
  if (number % 2 === 0) {
    console.log("Парне");
  } else {
    console.log("Непарне");
  }
}

// Потім створи функцію evaluateNumber(number, callback) і передай їй колбек.

function evaluateNumber(number, callback) {
  callback(number);
}
evaluateNumber(123, checkEvenOdd);
evaluateNumber(122, checkEvenOdd);
evaluateNumber(0, checkEvenOdd);

// =====================================================

const numbers = [5, 10, 15, 20, 25];

// Класичний for
for (let i = 0; i < numbers.length; i += 1) {
  console.log(`Index ${i + 1}, value ${numbers[i]}`);
}

// Перебираючий метод forEach
numbers.forEach(function (number, index) {
  console.log(`Index ${index + 1}, value ${number}`);
});
