// 🧩 ЗАВДАННЯ 1: Глобальний контекст
// Що буде виведено в консоль?
function showThis() {
  console.log(this);
}
showThis();
// 💬 Підказка: у строгому режимі undefined, інакше window

// 🧩 ЗАВДАННЯ 2: Контекст об’єкта
// Виведи ім’я користувача, використовуючи this
const user = {
  name: "Alex",
  showName() {
    console.log(this.name);
  },
};
user.showName();
// 💬 Підказка: метод викликається через крапку → this = user

// 🧩 ЗАВДАННЯ 3: call() vs apply()
// Використай обидва методи, щоб викликати функцію з контекстом person
function greet(age, city) {
  console.log(`${this.name}, ${age} років, ${city}`);
}
const person = { name: "Nastia" };

greet.call(person, 28, "Kyiv"); // Виклик через call
greet.apply(person, [28, "Kyiv"]); // Виклик через apply
// 💬 Підказка: call — аргументи через кому, apply — масивом

// 🧩 ЗАВДАННЯ 4: bind() — фіксуємо контекст
// Створи нову функцію, де this завжди дорівнює user
const user2 = { name: "Andrii" };
function showName() {
  console.log(this.name);
}

const boundFn = showName.bind(user2);
boundFn();
// 💬 Підказка: bind створює нову функцію, не викликає одразу

// 🧩 ЗАВДАННЯ 5: bind() у callback
// Кнопка викликає метод, але this може загубитися. Виправ bind’ом.
const counter = {
  value: 0,
  increment() {
    this.value++;
    console.log(this.value);
  },
};

// Імітація натискання кнопки:
const fakeBtnClick = counter.increment.bind(counter);
fakeBtnClick();
fakeBtnClick();
// 💬 Підказка: без bind this було б undefined

// 🧩 ЗАВДАННЯ 6: стрілочна функція та this
// Виправ функцію, щоб виводило ім’я користувача
const user3 = {
  name: "Oleh",
  show() {
    const arrow = () => console.log(this.name);
    arrow();
  },
};
user3.show();
// 💬 Підказка: стрілочна функція бере this із зовнішнього контексту
